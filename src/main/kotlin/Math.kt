fun main() {
    // rounds decimal number up to the next whole number
    println(Math.ceil(6.9))

    // rounds the number down to previous whole number
    println(Math.floor(6.9))

    // takes the absolute value of number
    println(Math.abs(-30))

    // square-root of number
    println(Math.sqrt(25.0))
}